# STILTS AGENT COMMANDLINE TOOL.

## Dependencies.
- STITLS (accesable via the command stilts in the terminal.) [link]

## Installation

```bash
conda env create -f environment.yml
conda activate stilts-agent
```

to use:

```
python stilts-agent
```
